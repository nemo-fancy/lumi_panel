name: CI

on:
  push:
    branches: ["**"]
  pull_request:

env:
  # Once CGO is linked in, cross-compilation and static linking are both gone,
  # and with them the single-binary promise. This is set globally so it cannot
  # be forgotten in one job.
  CGO_ENABLED: "0"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.24"
          cache: true

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "these files are not gofmt-clean:"
            echo "$unformatted"
            exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: test
        run: go test ./...

      # The ladder is shared mutable state behind a mutex and the event bus
      # dispatches under a read lock. Both are exactly the shapes where a
      # refactor introduces a race that normal test runs never surface.
      - name: test -race
        run: go test -race ./...

      - name: build
        run: go build -ldflags "-X main.version=${GITHUB_SHA::12}" ./cmd/lumipanel

      # Proves the single-binary claim on every commit rather than at release
      # time, when a CGO dependency has already been merged.
      - name: cross-compile
        run: |
          for target in linux/amd64 linux/arm64 darwin/arm64 windows/amd64; do
            GOOS=${target%/*} GOARCH=${target#*/} go build -o /dev/null ./cmd/lumipanel
          done

  schema:
    runs-on: ubuntu-latest
    # The Go tests never execute SQL, so nothing else in this pipeline would
    # notice a migration that parses and then fails on its first insert.
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready --health-interval 5s
          --health-timeout 5s --health-retries 10
    env:
      PGHOST: localhost
      PGUSER: postgres
      PGPASSWORD: postgres
    steps:
      - uses: actions/checkout@v4
      - name: install psql
        run: sudo apt-get update && sudo apt-get install -y postgresql-client
      - name: apply migrations and verify
        run: scripts/verify-schema.sh

  vulncheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.24"
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - run: govulncheck ./...
